In this directory are all files related to the ETL that incrementally
reads archive records from weewx (SQLite) and inserts those into Postgres.

weewx runs on a Raspberry Pi behind a NAT.
Postgres on a remote server.
